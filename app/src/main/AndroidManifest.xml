<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.knu.eattogether">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_my"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_my_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".EatOutChattingActivity"></activity>
        <activity android:name=".EatOutBoardDetailActivity" />
        <activity android:name=".WriteEatOutBoardActivity" />
        <activity android:name=".DeliveryChattingActivity" />
        <activity android:name=".MyChatListActivity" />
        <activity android:name=".MyPostsActivity" />
        <activity android:name=".OnlyImageActivity" />
        <activity android:name=".DeliveryBoardDetailActivity" />
        <activity android:name=".WriteDeliveryBoardActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FindActivity" />
        <activity android:name=".MainActivity" />

        <service android:name=".Notification.FirebaseNotificationService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>